{
  "name": "secret-santa",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "main": "server/index.js",
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "body-parser": "^1.20.0",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "firebase-admin": "^14.4.0",
    "nodemailer": "^10.0.10"
  },
  "scripts": {
    "start": "node server/index.js",
    "buildall": "cd app && npm run build",
    "test": "node --test --test-concurrency=1 test/*.test.js",
    "test:emulator": "firebase emulators:exec --only firestore --project demo-secret-santa \"npm test\""
  },
  "overrides": {
    "gaxios": {
      "uuid": "^11.1.1"
    }
  }
}
