{
  "name": "secret-santa",
  "version": "2.1.0",
  "private": true,
  "main": "index.js",
  "engines": {
    "node": "16"
  },
  "dependencies": {
    "body-parser": "^1.20.0",
    "core-js": "^3.21.1",
    "dotenv": "^16.0.0",
    "express": "^4.17.3",
    "firebase": "^9.6.10",
    "firebase-admin": "^9.5.0",
    "nodemailer": "^6.7.2",
    "react": "^17.0.0",
    "react-dom": "^17.0.0"
  },
  "scripts": {
    "build": "babel server -d lib",
    "start": "npm run build && node lib/index.js",
    "buildall": "npm run build && cd app && npm run build && cd .."
  },
  "devDependencies": {
    "@babel/cli": "^7.17.6",
    "@babel/core": "^7.17.8",
    "@babel/preset-env": "^7.16.11"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
